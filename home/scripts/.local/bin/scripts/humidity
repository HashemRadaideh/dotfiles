#!/usr/bin/env bash

humidity=$(openweather | jq -r '.main.humidity // empty')

if [ -z "$humidity" ] || [ "$humidity" = "null" ]; then
  echo "Error: Could not parse weather info from API response." >&2
  exit 1
fi

printf "%.0f%%\n" "$humidity"
