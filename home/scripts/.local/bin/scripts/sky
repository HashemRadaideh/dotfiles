#!/usr/bin/env bash

sky=$(openweather | jq -r '.weather[0].main // empty')

if [ -z "$sky" ] || [ "$sky" = "null" ]; then
  echo "Error: Could not parse weather info from API response." >&2
  exit 1
fi

printf "%s\n" "$sky"
